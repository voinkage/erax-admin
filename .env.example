# ═══════════════════════════════════════════════════════════════
# ERAX ADMIN BACKEND – Örnek Ortam Değişkenleri
# Kopyala: cp .env.example .env ve değerleri doldur
# ═══════════════════════════════════════════════════════════════

NODE_ENV=development
PORT=3010
DEFAULT_PORT=3010

# ─── JWT (Zorunlu) ───────────────────────────────────────────
# eradil-kullanici ile AYNI secret kullanılmalı (token doğrulama)
JWT_SECRET=your_jwt_secret_min_32_chars_here

# ─── Veritabanları ───────────────────────────────────────────
# Üye yönetimi (okullar, sınıflar, kullanıcılar, aktivasyon) – ZORUNLU
KULLANICI_DB_URL=postgresql://user:password@host:5432/kullanici_db
ORGANIZASYON_DB_URL=postgresql://user:password@host:5432/organizasyon_db

# Etkinlik Sihirbazı (etkinlikler, etkinlik_sorulari, soru_secenekleri) – Admin listesi/önizleme/CRUD
ICERIK_DB_URL=postgresql://user:password@host:5432/icerik_db

# Etkinlik silme sırasında öğrenci cevap kayıtlarının temizlenmesi (soru_cevaplari, ogrenci_etkinlik_cevaplari)
SEVIYE_DB_URL=postgresql://user:password@host:5432/seviye_db

# Ünite Sihirbazı (kitaplar, kitap_sorulari, kitap_soru_secenekleri) – Admin listesi/önizleme/CRUD
DIGIBUCH_DB_URL=postgresql://user:password@host:5432/digibuch_db

# DB bağlantı havuzu (opsiyonel)
# DB_CONNECTION_LIMIT=10
# DB_IDLE_TIMEOUT=30000
# DB_CONNECT_TIMEOUT=60000

# ─── CORS ───────────────────────────────────────────────────
# Frontend origin(leri), virgülle ayrılmış; * = tümü (sadece geliştirme)
CORS_ORIGIN=http://localhost:5173,https://www.eradil.online,https://eradil.online
CORS_CREDENTIALS=true

# ─── Güvenlik / Limit ────────────────────────────────────────
RATE_LIMIT=200
RATE_LIMIT_WINDOW_MS=60000
BODY_SIZE_LIMIT=10mb

# ─── Dosya Yükleme (Medya kütüphanesi, etkinlik/kitap görsel-ses) ─
# true = BunnyCDN, false = upload.php (eradil.online sunucusu)
USE_BUNNYCDN=true

# BunnyCDN (USE_BUNNYCDN=true ise)
BUNNYCDN_STORAGE_ZONE=erax-storage
BUNNYCDN_ACCESS_KEY=your_bunny_access_key
BUNNYCDN_PULL_ZONE=https://erax-cdn.b-cdn.net
# CDN dosya silme/rename için (opsiyonel)
# BUNNYCDN_API_KEY=your_bunny_api_key

# Eski upload sistemi (USE_BUNNYCDN=false ise)
# UPLOAD_URL=https://www.eradil.online/api/upload.php
# UPLOAD_SECRET=your_upload_secret_or_jwt_secret
